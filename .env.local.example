# Copy to .env.local and set your Google API Key
GOOGLE_API_KEY=your_google_api_key_here

# Optional: override model (use a model you have access to, e.g., "models/text-bison-001")
# If not set, defaults to: models/gemini-2.0-flash
# Example that forces Gemini 2.0 Flash explicitly (recommended if your key has access):
# GOOGLE_MODEL=models/gemini-2.0-flash
GOOGLE_MODEL=models/gemini-2.0-flash

# Optional: limit config
RATE_LIMIT_PER_MINUTE=30
